import React from "react";
import CompareMatrixClient from "@/components/CompareMatrixClient";

export const metadata = {
  title: "Compare AI Tools Side-by-Side | AuraAI",
  description: "Select and compare AI tools side-by-side to find the best platform for your workflows, comparing features, pros, cons, and pricing.",
};

export default function ComparePage() {
  return <CompareMatrixClient />;
}
